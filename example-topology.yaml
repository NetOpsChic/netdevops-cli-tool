project: "netdevops-lab-3"
start_nodes: true   # This will start all nodes after deployment

routers:
  - name: "R1"
    template: "c7200"
  - name: "ZTP"
    template: "ztp"

links:
  - endpoints:
      - name: "R1"
        adapter: 0
        port: 0
      - name: "ZTP"
        adapter: 0
        port: 0
